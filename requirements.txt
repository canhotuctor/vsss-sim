# Core runtime dependencies
numpy>=1.24
gymnasium>=0.29

# Optional: rendering (pip install vsss-sim[render])
# pygame>=2.4

# Optional: GPU acceleration via PyTorch (pip install vsss-sim[gpu])
# torch>=2.0

# Development / testing
pytest>=7.4
pytest-cov>=4.1
ruff>=0.4
